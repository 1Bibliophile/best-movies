export const Header = ({bigTitle, smallTitle}) => {
    return (
        <div id="header">
            <h1 id="big-title">{bigTitle}</h1>
            {/*<h2 id="small-title">{smallTitle}</h2>*/}
        </div>
    )
}
